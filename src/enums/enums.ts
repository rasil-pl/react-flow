export enum TOOL {
  MOVE = 'move',
  HAND = 'hand',
}

export enum NODE {
  BASIC = 'editableText',
  ACTOR = 'actor',
  CIRCLE = 'circle',
}

export enum EDGE {
  STRAIGHT = 'straight',
  BEZIER = 'bezier',
  SMOOTH_STEP = 'smoothStep',
}
