export enum TOOL {
  MOVE = 'move',
}

export enum NODE {
  BASIC = 'editableText',
  ACTOR = 'actor',
  CIRCLE = 'circle',
}

export enum EDGE {
  STRAIGHT = 'straight',
  BEZIER = 'bezier',
  SIMPLE_BEZIER = 'simpleBezier',
  SMOOTH_STEP = 'smoothStep',
  STEP = 'step',
}
